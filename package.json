{
  "name": "mock-data-generator",
  "version": "1.0.0",
  "description": "Generate Mock Data",
  "main": "index.js",
  "scripts": {
    "clean": "scripts/clean-dist.sh",
    "build": "npm run clean && echo Building... && tsc && echo Build Finished ./dist",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "Mock",
    "Data"
  ],
  "author": "Samuel Kosasih",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^9.2.0",
    "faker": "^6.6.6",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "prettier": "3.3.3"
  }
}